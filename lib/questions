how did you learn webpack?
do we have to do npm init/install webpack each time?
do we need to install the webpack server?
When writting the require statement in the entry file, do we need to set the const = to the name of the module.exports we created?
window.MovingObject ??
What if we make changes to the doc, do we delete the bundle file and redo the command?
config files, whats the best way to think of having multiple entry files
Why do we need to set the require statement = const in the file file as opposed to just having require?
Do we touch the bundle.js file once its been created?
what about the bundle.js.map file?


can you explain the util file?
what is POJO exactly?
addEventListener?
what is the document keyword?
isWrappable?
